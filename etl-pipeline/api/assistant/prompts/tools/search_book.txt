Search the selected book for relevant excerpts.

The book has been processed into a search index composed of the full text divided into short text chunks. 

The search tool returns the 10 excerpts with the most relevant content.

The search tool works by ranking all text chunks by the `ranking_query` and filtering out all text chunks that do not match the conditions in `filters`. The 10 highest ranking text chunks are returned.