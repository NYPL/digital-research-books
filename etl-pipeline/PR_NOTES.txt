* around testing, I wasn't sure whether it was really useful for a strict unit test of _on_max_turns becasue what I really care about is the functional integration that send the max turns response to run_result.final_output... so I created a unit and an integretion test that test the same kind of flow with and without mocking. 
*  test_runner_calls_on_max_turns: is the unit test just basically testing agents SDK and we should rely on that packages internal tests rather than testing the package. test_update_chat_max_turns_graceful_response is pretty much the same. in terms of internal logic, it asserts that _on_max_turns is passed to Runner.run in update_chat(). Is it useful to have thee 2 significantly duplicate tests?