* was PipelineBuilder intended to be the run_pipeline I created?
* The GlobalConfig mixes configuration of unrelated things: TP index, ES index, embedder. the rest of the code base uses env vars specified in teh config files and reads the env vars where needed. env vars can be overriden by local values with `var = var or os.getenv('VAR')` or `var = var or require_env('VAR')` structures where needed. @kunal I propose getting rid of this config entirely. what do you think?
the benefit of having a config is setting a value in one place and accessing it it in many, btw classes and btw models generally as classes and modules have their on shared access paradigms (__init__ and global vars respectively). SO there is a benefit for a config that isn't about external resource configuration in dev/qa/prod (which the .env files should be focused on, but is on shared configuratin across the pipeline in a way that we might not want to be fixed in a dev/qa/prod division e.g. running multiple pipeline configurations in the same environment). Another benefit of a config is a schema of those values you want to share across classes/modules
* should CachedS3Loader be the run_pipeline default loader?
* since there is already a scripts folder at he top level , I think we should bring `vector_indexing/scripts/` there rather than proliferate scripts/ folders